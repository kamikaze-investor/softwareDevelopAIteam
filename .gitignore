node_modules/
dist/
build/
.next/
.expo/

# 環境変数（絶対コミットしない）
.env
.env.local
.env.production
.env.*.local

# 秘密情報
*.pem
*.key
id_rsa
id_ed25519
service-account.json
credentials.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Logs
*.log
data/logs/

# 開発ログ: Git管理しないもの（長文・個人情報・APIキーリスク）
logs/conversations/
logs/implementation/
logs/codex/
logs/gemini-review/
logs/commands/

# 開発ログ: Git管理するもの（!で除外解除）
# logs/dev-sessions/   → Git管理（重要セッションのみ）
# logs/consultations/  → Git管理（承認済みのみ）
# docs/review-packets/ → Git管理（承認記録）

# Test coverage
coverage/

# pnpm
.pnpm-store/

# SQLite データベース本体・一時ファイル
apps/api/data/*.db
*.db-shm
*.db-wal

# Safety Audit 承認ログ（監査証跡はローカルのみ）
data/approvals/

# Meta Review ランタイム生成物（実行のたびに上書きされる）
meta-review-result.json
data/quota-exhausted.json

# Worker ランタイム生成物（実行ログ・stdout/stderrダンプ）
apps/worker/data/

# SQLite DB本体・一時承認ディレクトリ（data/README.md等の追跡ファイルは対象外）
data/*.db
data/.tmp_approval/
